apiVersion: v1
kind: Service
metadata:
  name: face-recognition-api
  namespace: face-recognition
spec:
  selector:
    app: face-recognition-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
