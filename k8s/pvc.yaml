apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: face-recognition-data
  namespace: face-recognition
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
